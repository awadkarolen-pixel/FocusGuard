# Python
__pycache__/
*.pyc
.venv/
venv/
.env

# Node / React
node_modules/
dist/
build/

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/

# DB / logs
*.sqlite
*.db
logs/

# YOLO model weights: ignore all except the one the app ships with
# (yolo11s.pt is used for phone detection and is committed so the project
# runs from a fresh clone without downloading it at startup).
*.pt
!src/backend/yolo11s.pt
